# Platform resources: the SSOApplication declarations the
# adomi-platform-controller reconciles. Kept separate from the controller chart
# so the operator (and its CRDs) install first, then these custom resources.
apiVersion: v2
name: platform-resources
description: SSOApplication declarations for the Adomi platform.
type: application
version: 0.1.0
appVersion: "0.1.0"