# The platform's own tenant. SSOApplications for platform components reference it.
apiVersion: platform.adomi.io/v1alpha1
kind: Tenant
metadata:
  name: {{ .Values.tenant.name | quote }}
spec:
  displayName: Platform
  namespace: {{ .Values.tenant.namespace | quote }}
  domains:
    - {{ .Values.domain | quote }}