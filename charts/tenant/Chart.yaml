apiVersion: v2
name: tenant
description: >-
  Per-customer GitOps bootstrap: an Argo CD AppProject that fences a customer's
  tenant repo to its own namespaces + the platform CRD kinds, plus the root
  Application that syncs that repo's intent (Client/Workspace/Application/Snapshot)
  into the cluster. One release per customer, generated by the `tenants`
  ApplicationSet from the Forgejo `tenants` org.
type: application
version: 0.1.0
appVersion: "1.0"
maintainers:
  - name: Adomi Software, LLC
    email: github@adomisoftware.com
